
# Чердак-ТВ — модульная система управления медиа-сервером

## 🎯 Цель
Создать **воспроизводимую, модульную и самодокументированную** систему настройки и управления медиа-сервером на базе Ubuntu 24.04 LTS, полностью управляемую через shell-скрипты.

---

## 🧱 Архитектурные принципы

### 1. **Единая точка входа**
- Корневой скрипт: `0.sh`
- Не зависит от содержимого модулей.
- Не содержит жёстко закодированных имён модулей.

### 2. **Модуль = каталог `NN_name/`**
- `NN` — две цифры (порядковый номер).
- Имя — осмысленное (например, `30_headless`, `70_media_servers`).
- Каждый модуль **самодостаточен**.

### 3. **Контракт модуля (`module.conf`)**
Каждый модуль **обязан** содержать файл `module.conf` со следующими полями:

```ini
name=Человекочитаемое название
icon=🎨 (один эмодзи)
short_status=путь/к/state_short.sh  # для главного экрана
manage=путь/к/manage.sh             # интерактивное управление
install=путь/к/install.sh            # автоматическая установка
default=путь/к/default.sh           # настройка "по умолчанию"
config=путь/к/config.local          # (опционально) локальный конфиг
```

Все пути — **относительно корня проекта** (`~/cherdak_tv/`).

### 4. **Стандартные интерфейсы модуля**
Каждый модуль **должен** реализовывать (если применимо):

| Файл | Назначение |
|------|-----------|
| `state_short.sh` | Краткий статус (одна строка, цветной) |
| `state.sh` | Подробный статус |
| `manage.sh` | Интерактивное меню управления |
| `install.sh` | Установка зависимостей |
| `default.sh` | Настройка "по умолчанию" (с диалогом при необходимости) |
| `autostart.sh` | Скрипт автозапуска при загрузке |
| `docker-compose-NN_name.yml` | (если нужен Docker) — будет включён в общий `docker-compose.yml` |

### 5. **Общие библиотеки**
- Располагаются в каталоге `tools/`.
- Содержат утилиты: цвета, диалоги, проверки.
- Подключаются через `source`.

### 6. **Docker-интеграция**
- Если модуль использует Docker — он предоставляет `docker-compose-NN_name.yml`.
- Корневой скрипт **автоматически собирает** единый `docker-compose.yml` из всех модулей в порядке сортировки по имени каталога.

### 7. **Генерация `.env`**
- Корневой скрипт может сгенерировать `.env`:
  - IP-адрес определяется автоматически (`hostname -I`).
  - Пароли запрашиваются у пользователя в интерактивном режиме.
- Использует данные из глобального `config.sh` (если есть).

### 8. **Системное обслуживание**
Корневой скрипт предоставляет:
- `apt update / upgrade / autoremove`
- Активацию **Ubuntu Pro** (`sudo pro attach`)
- Управление обновлениями безопасности

### 9. **Git-ориентированность**
- Все изменения — через коммиты.
- Работа над модулем — в отдельной ветке: `feature/module-NN_name`.
- Промпты и решения — фиксируются в истории.

---

## 📂 Структура проекта

```
cherdak_tv/
├── 0.sh                     # Единая точка входа
├── README.md                # Этот файл
├── .gitignore
├── .env                     # (генерируется)
├── docker-compose.yml       # (генерируется из модулей)
│
├── tools/                   # Общие библиотеки
│   ├── colors.sh
│   ├── dialog.sh
│   └── utils.sh
│
├── 10_system/               # Пример модуля
│   ├── module.conf
│   ├── state_short.sh
│   └── ...
│
└── 30_headless/             # VNC/RDP
    ├── module.conf
    ├── state_short.sh
    ├── manage.sh
    ├── autostart.sh
    └── ...
```

---

## ▶️ Использование

1. Клонируй или создай репозиторий.
2. Выполни:
   ```bash
   ./0.sh
   ```
3. Следуй меню:
   - Просмотр статусов,
   - Управление модулями,
   - Установка/настройка,
   - Системное обслуживание.

---

## 🛠️ Требования

- Ubuntu 24.04 LTS
- Bash 5+
- `git` (для работы с репозиторием)

---

## 💡 Примечания

- **Корневой скрипт ничего не знает о модулях** — работает только через `module.conf`.
- **Добавление нового модуля** = создать каталог `NN_name/` + `module.conf` → он автоматически появится в меню.
- **Безопасность**: пароли не хранятся в `.env` в открытом виде без необходимости.

---

> «Чердак-ТВ» — это не просто скрипты, а **инфраструктурный код**, управляемый как программный проект.
